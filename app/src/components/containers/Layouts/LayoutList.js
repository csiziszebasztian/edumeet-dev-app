import React from 'react';
import LayoutCards from './LayoutCards';

const LayoutList = () => {
    return (
        <div>
            <LayoutCards/>
            <LayoutCards/>
            <LayoutCards/>
        </div>
    );
}

export default LayoutList;